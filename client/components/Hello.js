import React, { Fragment } from 'react'
import { hot } from 'react-hot-loader'

const hello = () => (
    <Fragment>
        <div>Hello jgjh</div>
    </Fragment>
)

export default hot(module)(hello)
